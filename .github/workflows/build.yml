name: Build and Push Snell Docker Image

on:
  schedule:
    # 每小时检查一次新版本
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      version:
        description: 'Snell版本号（如：v5.0.1、v5.0.0b2，留空使用最新版本）'
        required: false
        type: string
      debian:
        description: 'Debian版本（如：bookworm, bullseye, trixie）'
        required: false
        default: 'bookworm'
        type: string
      use_repo_files:
        description: '使用仓库中的 Version 目录文件（如果存在）'
        required: false
        type: boolean
        default: false
      force:
        description: '强制构建（即使版本已存在）'
        required: false
        type: boolean
        default: false

env:
  DOCKER_HUB_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
  GHCR_USERNAME: ${{ github.repository_owner }}

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.version }}
      should_build: ${{ steps.check_build.outputs.should_build }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 获取 Snell 版本
        id: get_version
        run: |
          normalize() {
            v="$1"
            [[ "$v" != v* ]] && v="v$v"
            echo "$v"
          }

          if [ -n "${{ inputs.version }}" ]; then
            VERSION="$(normalize "${{ inputs.version }}")"
            echo "✓ 使用指定版本: $VERSION"
          else
            echo "→ 检测最新版本..."
            VERSION=$(curl -sL https://kb.nssurge.com/surge-knowledge-base/zh/release-notes/snell | \
              grep -oP 'snell-server-v\K[0-9]+\.[0-9]+\.[0-9]+([a-z]+[0-9]+)?' | \
              sed 's/^/v/' | sort -Vru | head -n 1)

            if [ -z "$VERSION" ]; then
              VERSION=$(curl -sL https://dl.nssurge.com/snell/ 2>/dev/null | \
                grep -oP 'snell-server-v\K[0-9]+\.[0-9]+\.[0-9]+([a-z]+[0-9]+)?' | \
                sed 's/^/v/' | sort -Vru | head -n 1)
            fi

            if [ -z "$VERSION" ]; then
              echo "✗ 无法获取版本号"
              exit 1
            fi

            echo "✓ 检测到最新版本: $VERSION"
          fi

          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: 检查仓库中是否有该版本的文件
        id: check_repo_files
        run: |
          VERSION="${{ steps.get_version.outputs.version }}"
          USE_REPO="${{ inputs.use_repo_files }}"

          if [ "$USE_REPO" = "true" ] || [ -z "$USE_REPO" ]; then
            if ls Version/${VERSION}/snell-server-${VERSION}-linux-*.zip 1> /dev/null 2>&1; then
              echo "✓ 仓库中存在版本 ${VERSION} 的文件"
              echo "has_files=true" >> $GITHUB_OUTPUT
            else
              echo "→ 仓库中不存在版本 ${VERSION} 的文件"
              echo "has_files=false" >> $GITHUB_OUTPUT
            fi
          else
            echo "→ 不使用仓库文件"
            echo "has_files=false" >> $GITHUB_OUTPUT
          fi

      - name: 检查是否需要构建
        id: check_build
        run: |
          VERSION="${{ steps.get_version.outputs.version }}"
          FORCE="${{ inputs.force }}"

          if [ "$FORCE" = "true" ]; then
            echo "✓ 强制构建模式"
            echo "should_build=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          if docker manifest inspect ghcr.io/${{ github.repository_owner }}/snell:$VERSION >/dev/null 2>&1; then
            echo "→ 版本 $VERSION 已存在于 GHCR"
            echo "should_build=false" >> $GITHUB_OUTPUT
          else
            echo "✓ 版本 $VERSION 需要构建"
            echo "should_build=true" >> $GITHUB_OUTPUT
          fi

  build-and-push:
    needs: check-version
    if: needs.check-version.outputs.should_build == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        if: env.DOCKER_HUB_USERNAME != ''
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 准备构建参数
        id: prep
        run: |
          VERSION="${{ needs.check-version.outputs.version }}"
          DEBIAN="${{ inputs.debian || 'bookworm' }}"
          USE_REPO="${{ inputs.use_repo_files }}"

          if [ "$USE_REPO" = "true" ]; then
            if ls Version/${VERSION}/snell-server-${VERSION}-linux-*.zip 1> /dev/null 2>&1; then
              USE_LOCAL="true"
              echo "✓ 将使用仓库中的文件"
            else
              USE_LOCAL="false"
              echo "→ 仓库中无文件，将从官方下载"
            fi
          else
            USE_LOCAL="false"
            echo "→ 将从官方下载"
          fi

          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "debian=$DEBIAN" >> $GITHUB_OUTPUT
          echo "use_local=$USE_LOCAL" >> $GITHUB_OUTPUT

          GHCR_IMAGE="ghcr.io/${{ github.repository_owner }}/snell"
          TAGS="${GHCR_IMAGE}:${VERSION},${GHCR_IMAGE}:latest"

          if [ -n "${{ secrets.DOCKER_HUB_USERNAME }}" ]; then
            DOCKER_HUB_IMAGE="${{ secrets.DOCKER_HUB_USERNAME }}/snell"
            TAGS="${TAGS},${DOCKER_HUB_IMAGE}:${VERSION},${DOCKER_HUB_IMAGE}:latest"
          fi

          echo "tags=$TAGS" >> $GITHUB_OUTPUT

          echo "📦 构建信息:"
          echo "  版本: $VERSION"
          echo "  Debian: $DEBIAN"
          echo "  使用仓库文件: $USE_LOCAL"
          echo "  标签: $TAGS"

      - name: 创建空 Version 目录（如果不存在）
        run: |
          mkdir -p Version
          touch Version/.gitkeep

      - name: 列出 Version 目录内容
        run: |
          echo "→ Version 目录内容:"
          ls -lhR Version/ || echo "  (目录为空)"

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/386,linux/arm64,linux/arm/v7
          push: true
          tags: ${{ steps.prep.outputs.tags }}
          build-args: |
            SNELL_VERSION=${{ steps.prep.outputs.version }}
            DEBIAN_VERSION=${{ steps.prep.outputs.debian }}
            USE_LOCAL=${{ steps.prep.outputs.use_local }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 创建 Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.prep.outputs.version }}
          name: Snell ${{ steps.prep.outputs.version }}
          body: |
            ## 🚀 Snell Server ${{ steps.prep.outputs.version }}

            **基础镜像:** Debian ${{ steps.prep.outputs.debian }}
            **构建方式:** ${{ steps.prep.outputs.use_local == 'true' && '使用仓库文件' || '官方下载' }}

            ### 📦 镜像地址

            **GitHub Container Registry:**
            ```bash
            docker pull ghcr.io/${{ github.repository_owner }}/snell:${{ steps.prep.outputs.version }}
            docker pull ghcr.io/${{ github.repository_owner }}/snell:latest
            ```

            **Docker Hub:** ${{ secrets.DOCKER_HUB_USERNAME != '' && format('
            ```bash
            docker pull {0}/snell:{1}
            docker pull {0}/snell:latest
            ```', secrets.DOCKER_HUB_USERNAME, steps.prep.outputs.version) || '（未配置）' }}

            ### 🏗️ 支持平台
            - `linux/amd64`
            - `linux/386`
            - `linux/arm64`
            - `linux/arm/v7`
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 构建摘要
        run: |
          echo "### ✅ 构建完成" >> $GITHUB_STEP_SUMMARY
          echo "**版本:** ${{ steps.prep.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "**Debian:** ${{ steps.prep.outputs.debian }}" >> $GITHUB_STEP_SUMMARY
          echo "**构建方式:** ${{ steps.prep.outputs.use_local == 'true' && '使用仓库文件' || '官方下载' }}" >> $GITHUB_STEP_SUMMARY
          echo "**镜像标签:**" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.prep.outputs.tags }}" | tr ',' '\n' >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
